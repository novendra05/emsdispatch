Config = {}

Config.EMSJobs = {
    ['ambulance'] = true,
    ['ems'] = true
}

-- Keybind for history (Standard: U)
Config.Keybind = 'U'

-- Database cleanup (Auto-delete calls older than X days)
Config.AutoCleanup = 7
